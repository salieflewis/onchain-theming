import { Inter, Roboto, Rubik_Glitch } from '@next/font/google';

export const inter = Inter({
  weight: '400',
  subsets: ['latin'],
});

export const roboto = Roboto({
  weight: '400',
  subsets: ['latin'],
});

export const rubikGlitch = Rubik_Glitch({
  weight: '400',
  subsets: ['latin'],
});
